@model MarketForYou.Models.ReportErros

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ReportErros</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="reportId" />
            <div class="form-group">
                <label asp-for="anexo" class="control-label"></label>
                <input asp-for="anexo" class="form-control" />
                <span asp-validation-for="anexo" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="descricaoErro" class="control-label"></label>
                <input asp-for="descricaoErro" class="form-control" />
                <span asp-validation-for="descricaoErro" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="usernameCliente" class="control-label"></label>
                <input asp-for="usernameCliente" class="form-control" />
                <span asp-validation-for="usernameCliente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
